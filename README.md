# Env Var Confessional

A simple bash script that checks if your environment variables are present and accounted for. Run it before debugging to save hours of frustration.

Usage: `./env_confessional.sh [.env.example]`